import { colors } from "./colors";
import { mediaQuery } from "./media-queries";
import { zIndices } from "./z-index";

export const theme = {
  colors,
  fontSizes: {
    small: "1.2rem",
    large: "2rem",
    xl: "3rem",
  },
  mediaQuery,
  zIndices,
};
